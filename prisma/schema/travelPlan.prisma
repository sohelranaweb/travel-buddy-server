// model TravelPlan {
//         id            String               @id @default(uuid())
//         travelerId    String
//         destination   String // Country/City
//         startDate     DateTime
//         endDate       DateTime
//         budgetMin     Float?
//         budgetMax     Float?
//         travelType    TravelType
//         description   String?
//         isCompleted   Boolean              @default(false) // Trip complete হয়েছে কিনা
//         traveler      Traveler             @relation(fields: [travelerId], references: [id])
//         buddyRequests TravelBuddyRequest[]
//         travelBuddies TravelBuddy[]
//         createdAt     DateTime             @default(now())
//         updatedAt     DateTime             @updatedAt

//         @@index([travelerId])
//         @@index([destination])
//         @@index([isCompleted])
//         @@map("traveler_plans")
// }
// model TravelPlan {
//         id          String     @id @default(uuid())
//         travelerId  String
//         destination String
//         startDate   DateTime
//         endDate     DateTime
//         budgetMin   Int
//         budgetMax   Int
//         travelType  TravelType
//         description String?    @db.Text
//         isCompleted Boolean    @default(false) // Trip complete হয়েছে কিনা
//         createdAt   DateTime   @default(now())
//         updatedAt   DateTime   @updatedAt

//         // Relations
//         traveler      Traveler             @relation(fields: [travelerId], references: [id], onDelete: Cascade)
//         buddyRequests TravelBuddyRequest[]
//         travelBuddies TravelBuddy[]

//         @@index([travelerId])
//         @@index([destination])
//         @@index([isCompleted])
//         @@map("travel_plans")
// }

// TRAVEL PLAN MODEL
model TravelPlan {
        id          String     @id @default(uuid())
        travelerId  String
        destination String
        startDate   DateTime
        endDate     DateTime
        budgetMin   Float?
        budgetMax   Float?
        travelType  TravelType
        description String?
        isCompleted Boolean    @default(false)
        createdAt   DateTime   @default(now())
        updatedAt   DateTime   @updatedAt

        // Relations
        traveler      Traveler             @relation(fields: [travelerId], references: [id], onDelete: Cascade)
        buddyRequests TravelBuddyRequest[]
        travelBuddies TravelBuddy[]

        @@index([travelerId])
        @@index([destination])
        @@index([isCompleted])
        @@map("travel_plans")
}
