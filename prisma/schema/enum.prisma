enum UserRole {
        SUPER_ADMIN
        ADMIN
        TRAVELER
}

enum UserStatus {
        ACTIVE
        BLOCKED
        DELETED
}

enum Gender {
        MALE
        FEMALE
}

enum SubscriptionStatus {
        ACTIVE
        PENDING
        EXPIRED
        CANCELED
}

enum PaymentStatus {
        PAID
        UNPAID
}

enum TravelType {
        SOLO
        FAMILY
        FRIENDS
}

enum BuddyStatus {
  ACTIVE      // Currently traveling together বা trip শুরু হয়নি
  COMPLETED   // Trip completed - এখন review দেওয়া যাবে
  CANCELLED   // Buddy left or removed

  @@map("buddy_status")
}

enum RequestStatus {
  PENDING
  ACCEPTED
  REJECTED
  CANCELLED

  @@map("request_status")
}