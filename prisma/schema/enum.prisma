enum UserRole {
        SUPER_ADMIN
        ADMIN
        TRAVELER
}

enum UserStatus {
        ACTIVE
        BLOCKED
        DELETED
}

enum Gender {
        MALE
        FEMALE
}

enum SubscriptionStatus {
        ACTIVE
        PENDING
        EXPIRED
        CANCELED
}

enum PaymentStatus {
        PAID
        UNPAID
}
